# ─── Vercel CLI upload exclusions ─────────────────────────────────────────────
# These patterns only apply when deploying via `vercel` CLI.
# Git-integration deployments ignore this file entirely.
#
# IMPORTANT: Do NOT exclude any frontend source / config files here — they are
# required by the buildCommand ("cd frontend && npm install && npm run build").

# Vercel CLI generated folder
.vercel/

# Python virtual environments
.venv/
backend/.venv/
venv/
env/

# Python bytecode
__pycache__/
**/__pycache__/
**/*.pyc
**/*.pyo

# Local dev / editor files
.pytest_cache/
.git/
.gitignore
.vscode/

# Local node_modules (Vercel runs npm install during build)
node_modules/
frontend/node_modules/

# Documentation & planning (not needed for build or runtime)
plans/
LICENSE
