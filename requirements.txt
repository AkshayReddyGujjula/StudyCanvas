# Web framework & server
fastapi>=0.115.0
uvicorn>=0.34.0
pydantic>=2.10.0
slowapi>=0.1.9

# PDF extraction — pure-Python, works on Vercel serverless (no native binaries)
pypdf>=5.0.0
# pymupdf4llm is intentionally excluded here: PyMuPDF ships ~150 MB of native
# binaries which exceed Vercel's 249 MB Lambda limit.  Install it separately in
# your local venv if you want higher-quality Markdown extraction:
#   pip install pymupdf4llm
# pdf_service.py detects its presence at runtime and uses it automatically.

# AI / LLM
# Uses the unified google-genai SDK (replaces the deprecated google-generativeai package).
# Migration guide: https://ai.google.dev/gemini-api/docs/migrate
google-genai>=1.0.0

# FastAPI file-upload support
python-multipart>=0.0.20

# Environment variable loading
python-dotenv>=1.0.0

# Office document conversion: .docx and .pptx → PDF
# All pure-Python or wheel-only — no system binaries required on Vercel.
python-docx>=1.1.0
python-pptx>=1.0.0
reportlab>=4.2.0
# docx2pdf: uses Microsoft Office COM on Windows/macOS for exact "Save As PDF"
# quality; falls back to LibreOffice on Linux. Fails gracefully if neither is
# available (Vercel), where the ReportLab fallback is used instead.
docx2pdf>=0.1.8
# comtypes: pure-Python Windows COM automation — used by _try_powerpoint_com to
# convert .pptx via PowerPoint "Save As PDF" (identical to manual export).
# Safe to install on non-Windows: it's a no-op there. Not available on Vercel
# (Linux), where the ReportLab fallback is used instead.
comtypes>=1.4.0
