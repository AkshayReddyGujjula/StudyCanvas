# Web framework & server
fastapi>=0.115.0
uvicorn>=0.34.0
pydantic>=2.10.0
slowapi>=0.1.9

# PDF extraction — pure-Python, works on Vercel serverless (no native binaries)
pypdf>=5.0.0
# pymupdf4llm is intentionally excluded here: PyMuPDF ships ~150 MB of native
# binaries which exceed Vercel's 249 MB Lambda limit.  Install it separately in
# your local venv if you want higher-quality Markdown extraction:
#   pip install pymupdf4llm
# pdf_service.py detects its presence at runtime and uses it automatically.

# AI / LLM
# Uses the unified google-genai SDK (replaces the deprecated google-generativeai package).
# Migration guide: https://ai.google.dev/gemini-api/docs/migrate
google-genai>=1.0.0

# FastAPI file-upload support
python-multipart>=0.0.20

# Environment variable loading
python-dotenv>=1.0.0
