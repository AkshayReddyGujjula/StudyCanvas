# Local development only — this file is NOT used by Vercel.
# In production, VITE_API_BASE_URL is intentionally unset so the frontend
# uses relative URLs (same domain as the backend).
VITE_API_BASE_URL=http://localhost:8000
